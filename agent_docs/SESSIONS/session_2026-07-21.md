# Session Log: 2026-07-21

## Project Context
- **Project:** Cyph Mission Control (repo: cyber-pharma-admin-portal-v1)
- **Tool:** Claude Code
- **Goal:** Resume STAGING deploy — clear the `output: "standalone"` blocker, then hand off to next-deploy-execute

## Starting State
- **Branch:** `gcs-deploy-2` (clean; operator committed the 5-file package on 663e0c0 "14july2026 - deploy sh, docker file etc. created")
- **Last Working Feature:** everything (Phase 1 closed); GCP project `cyberize-nextjs-staging` bootstrapped + verified (2026-07-14)
- **Known Issues:** 🔴 next.config.js missing `output: "standalone"` (Dockerfile hard requirement) — operator APPROVED adding it this session

## Carryover (from session_2026-07-14.md)
Path A bootstrap COMPLETE · generate Phase 2 COMPLETE (5 files at root, all EVIDENCE in
response_2026-07-14_150210_generate-phase2-result.md) · sitting at Phase-2 stop gate.
Locked: app `cyph-mission-ctrl` · service `cyph-mission-ctrl-prod` · domain
mission-portal.cyberizedev.com · invoker PUBLIC · 1 runtime secret (supabase-secret-key).

## Session Progress

### [12:27] — APPROVED → IN PROGRESS
**Task:** Add `output: "standalone"` to next.config.js (operator-approved one-line change),
verify via local `npm run build` that `.next/standalone/` materializes and route table is intact.

### [12:3x] — COMPLETE: standalone blocker cleared
**Task:** `output: "standalone"` added to next.config.js (one line, nothing else touched)
**Tests:** `npm run build` GREEN — compiled 4.6s, tsc clean, 12/12 static pages. Route table
EXACT match to Phase-1 baseline (/, /login, 8 MC screens, api/auth/{login,logout,confirm},
_not-found, +icon.png). EVIDENCE: `.next/standalone/server.js` exists → Dockerfile Stage 3
dependency satisfied.
**Files changed:** next.config.js only.
→ generate skill Phase 3: handoff to next-deploy-execute presented; awaiting "deploy now".

### [14:37] — next-deploy-execute ENGAGED — Phase 0 pre-flight
Operator committed next.config.js change + said "deploy now". SKILL.md read end-to-end.
Phase 0 presented: files EVIDENCE-present, routing 1→2→3→(4?)→5→6→7 proposed.
Open: confirm init-never-run + first-deploy, and QUESTION #4 (cyberizedev.com already
Google-verified for the deploying account? decides Phase 4 skip).
Artifact: response_2026-07-21_143705_execute-phase0-preflight.md
**Status:** Awaiting routing confirmation + "Proceed to Phase 1"

### [14:4x] — Phase 0 CLOSED → Phase 1 IN PROGRESS
Operator confirmed: init never run · first deployment · domain verification UNSURE (assume
NOT verified → Phase 4 stays in the route). Routing locked: 1→2→3→4→5→6→7.

### [15:0x] — Phase 1 COMPLETE (one-time app setup)
- ./init-app.sh run 1: secret + runtime SA created; binding FAILED on IAM propagation race
  (400 "SA does not exist" ms after creation; set -e killed script pre-verification).
- Re-run (idempotent): binding GRANTED — EVIDENCE: policy shows runtime SA with
  secretmanager.secretAccessor; verification block green (secret listed, SA exists).
- Real secret value added by operator (version 2, echo -n, spot-check confirmed in
  operator terminal — operator-verified).
- Ledger: init-app.sh template needs retry/sleep between SA create and IAM bind.

### [15:2x] — Phase 3 first attempt FAILED at gcloud arg parsing → deploy.sh FIXED
`./deploy.sh` run 1: gcloud builds submit rejected substitutions — TEMPLATE BUG: TEMPLATES.md
deploy.sh splits --substitutions across `,\` continuation lines; bash keeps each line's leading
indent → separate argv tokens → "unrecognized arguments". Bites when >1 substitution line.
FIX: build one SUBSTITUTIONS string (+= per var), pass as single quoted arg. bash -n clean.
→ Ledger: TEMPLATES.md deploy.sh execution block is broken as written.

### [15:xx] — Phase 3 deploy SUCCESS + invoker saga (Phase 6 pulled forward)
- ./deploy.sh run 2: Cloud Build SUCCESS 10m17s (build d91e573b) · service cyph-mission-ctrl-prod
  revision 00001-l6d serving · run.app URL: https://cyph-mission-ctrl-prod-524380376459.us-east1.run.app
- WARNING at deploy step: "Setting IAM policy failed" — --allow-unauthenticated attempt by
  Actor B rejected → browser 403 Forbidden (expected: no invoker binding).
- ROOT CAUSE (INFERENCE, high confidence): project post-2024 secure defaults — build SA has no
  Editor/run-admin, unlike operator's older projects where the flag Just Worked. Operator
  independently ruled out org-policy domain restriction (allValues: ALLOW — EVIDENCE).
- FIX: Actor A ran add-iam-policy-binding allUsers/run.invoker → EVIDENCE: policy shows binding
  (add output + independent get-iam-policy). Phase 6 requirement SATISFIED (intent PUBLIC, matched).
- OPEN: one-time grant of run-admin-class role to build SA (project-level, so future apps incl.
  main Cyber Pharma deploy without the manual unlock) — build SA email query pending.
- LEDGER (operator-ruled, unqualified): Path A bootstrap for staging projects MUST grant build SA
  run.admin right after project creation. Also: warn operator pre-deploy that first URL 403s
  until invoker binding lands (this session's friction point).

### Phase 2 assessment — IAM verification
- Runtime SA (Actor C) binding: EVIDENCE (this session, pasted policy). ✅
- Build SA (Actor B): GAP — unqueryable, no builds yet. STRUCTURALLY IRRELEVANT: this app has
  ZERO build-time secrets → no build-SA secret bindings exist to verify. Phase 2 satisfiable
  without a truth command; deploy failure mode it guards is impossible here.

### [session close, logged 2026-07-22] — Phase 3 CLOSED GREEN (operator smoke test)
Operator verdict (next day): login + dashboard + charts + seed data GREEN on run.app URL ·
SSR cookie session works over https · role gate verified live (admin/member correctly rejected
with super-admin-only message) · known noise: Chrome Safe Browsing flags fresh *.run.app URL
("Dangerous", autofill weirdness, incognito clean) — not an app defect, retired by custom domain.
Decisions: Phase 4 stays assume-unverified (run only if mapping complains) · proceed Phase 5 ·
DNS = DigitalOcean · spoon-feed rhythm (one command per step). Continued in session_2026-07-22.md.

## Lessons Learned
- IAM propagation race: SA created then bound ms later → 400 "does not exist"; idempotent re-run
  fixes; template needs sleep/retry between SA create and bind.
- TEMPLATES.md deploy.sh `--substitutions` continuation-line pattern is broken with >1 var line
  (bash keeps leading indent → separate argv) — single-string SUBSTITUTIONS var is the fix.
- Post-2024 GCP secure defaults: build SA can't set IAM policy → --allow-unauthenticated warns,
  service 403s until Actor A binds invoker. STAGING-PROJECT RULE (operator): grant build SA
  run.admin at bootstrap, always. Also: pre-warn operator about the interim 403.
- Chrome Safe Browsing distrusts fresh *.run.app URLs — expect "Dangerous" noise pre-domain.

## End of Session State
- **Working:** everything + standalone build verified
- **Broken:** nothing
- **Next Steps:** operator runs DEPLOYMENT_CHECKLIST §1–2 (init-app.sh + real secret value) with
  agent guiding via next-deploy-execute

## Files Changed This Session
- `next.config.js` — add output: "standalone"
- this file
