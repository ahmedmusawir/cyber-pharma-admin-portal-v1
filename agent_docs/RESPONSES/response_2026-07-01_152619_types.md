# FFM Sub-Phase 1 — TYPES (MissionControl / cyph-mission-control-v1)

> Status: **COMPLETE — awaiting approval before Sub-Phase 2 (Services).** No logic/implementations authored.
> Generated: 2026-07-01 15:26:19 · Gate: `npx tsc --noEmit` → **CLEAN**.

## What was authored (`src/types/mission-control/`)
A cohesive, deletable module (barrel-exported) matching DATA_CONTRACT 1:1, minus the confirmed is_super_admin ruling.

| File | Maps to | Contents |
|---|---|---|
| `status.ts` | DATA_CONTRACT §1 | 7 string-literal unions: `AccountStatus`, `MemberRole`, `BusinessStatus`, `SubscriptionStatus`, `StoreHealth`, `RegistrationStatus`, `RegistrationType` |
| `entities.ts` | §2, §3, §3.7 | `SuperAdminUser`; view-models `OwnerSummary/OwnerDetail`, `StoreSummary/StoreDetail`, `Member`, `PendingRegistrationSummary/Detail`, `PlatformStats`, `GrowthPoint`, `AuditAction`, `AuditEntry`, `ActionResult` |
| `services.ts` | §4 | the 7 frozen interfaces: `MissionDashboardService`, `OwnerDirectoryService`, `StoreDirectoryService`, `StoreMemberService`, `SupportActionService`, `PendingRegistrationService`, `AuditLogService` |
| `index.ts` | — | barrel; explicit `export type { … }` (safe under `isolatedModules`) |

- **Consume path:** `import type { … } from "@/types/mission-control"`.
- Pure type surface — zero runtime, zero imports of runtime values. `import type` used internally.

## Rulings + scope clarifications baked in
- **is_super_admin ruling (confirmed):** `SuperAdminUser.isSuperAdmin: true` kept as the type invariant, documented as a server-derived `(kitRole === 'superadmin')` value — NOT a read of any `is_super_admin` column. No such column is referenced anywhere.
- **One operator role:** no admin/member operator variants, no role-branching or role-gated view types. `MemberRole ('admin'|'user')` is retained — it is data about *managed* stores/members, not operator gating.
- **RED list absolute (product definition, not role-hiding):** no `create-*`, no password set/read field, no email-entry/edit field, no billing/checkout/payment shape, no delete-human, no impersonation shape — anywhere, for anyone.
- **No account origination:** no create surface in any service; `SupportActionService` = suspend/unsuspend/recovery-trigger/resend/restore-admin; `PendingRegistrationService` = list/get/approve/reject only.

## Contract-fidelity notes (1:1 with DATA_CONTRACT)
- Unions (not TS enums) exactly as specified. Optionality preserved (`memberCount?`, `npi?`, `state?`, `jobTitle?`, `inviteStatus?`, converter-only `desktopUsername?`/`linkedBusiness?`, etc.).
- `AuditAction` closed to the 7 GREEN actions (no created_*/deleted_*).
- `approve`/`reject` take only `{registrationId, verificationNote|reason}` — never an email argument (load-bearing onboarding constraint).
- Derivation logic (StoreHealth precedence, pill→variant mapping) intentionally NOT modeled — types are shapes; the service computes, the UI never re-derives.

## Gate result
- `npx tsc --noEmit` → **CLEAN** (zero errors).
- RED-list self-audit grep → only comment hits + `created_at` (a Frank source-field name) + `sendPasswordRecovery` (the sanctioned GREEN recovery-*trigger*, APP_BRIEF §5.4). No forbidden shapes.

## Files
```
src/types/mission-control/status.ts
src/types/mission-control/entities.ts
src/types/mission-control/services.ts
src/types/mission-control/index.ts
```
Existing `src/types/{posts.ts,tailwind-merge.d.ts}` untouched.

## Ledger (noted, not acted)
- FFM DATA_CONTRACT §2 + APP_BRIEF §8 describe `is_super_admin` as a column — doc drift to correct in a later pass.

🛑 STOP — awaiting approval before Sub-Phase 2 (Services).
