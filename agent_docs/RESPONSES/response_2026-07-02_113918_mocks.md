# FFM Sub-Phase 3 — MOCKS (MissionControl / cyph-mission-control-v1)

> Status: **COMPLETE — awaiting approval before Sub-Phase 4 (Components).**
> Generated: 2026-07-02 11:39:18 · Gates: `npx tsc --noEmit` CLEAN · invariant demo PASS · baseline 81/81 · PHI-clean.

## What was authored
Seeded `src/mocks/mission-control/store.ts` (the seam from Sub-Phase 2) with a business-shaped SOURCE seed, then **projected/derived** the view-model collections and **derived** the dashboard stats from that seed — so every number is backed by data (no decorative counts). No service signatures touched.

## Coverage vs DATA_CONTRACT §5 (all states present)
- **Owners = 6:** Marcus (multi-store, **5**), Tina (single), Rosa (has a **past_due** store → health past_due), Omar (has a **suspended** store → health suspended), Lena (unpaid → past_due), Grace (**locked-out** owner of record).
- **Locked-out restore-admin target:** `sto-ivy` — Grace is owner of record but sits on the roster as a plain `user` (no active admin row) → restore-admin has a real target.
- **Members across all 3 AccountStatus:** `sto-cedar` roster = admin + active + **suspended** (Ben) + **invite_pending** (Carol). Also Nadia (suspended) and Karl (invite_pending) elsewhere.
- **Stores = 12, all 3 StoreHealth:** active (majority), **past_due** (Willow=past_due sub, Fern=unpaid), **suspended** (Spruce=suspended business). Subscription statuses span active/trialing/canceled/past_due/unpaid.
- **Pending registrations = 7:** both types; statuses span `pending_verification` (×3), `approved`, `rejected`, `expired`, `completed`. Converters carry `desktopUsername` + `linkedBusiness`.
- **Audit:** 7 seeded rows covering **every** AuditAction.
- **Dashboard:** stats derived from seed; `getGrowth(6)` sums to totalPharmacies.

## Consistency proof (from the invariant demo — reads)
```
owners 6 · stores 12 · cedarRoster 4 · pendingDefault 3 · auditSeed 7 · growthSum 12
stats: totalPharmacies 12 · totalOwners 6 · totalMembers 13
       activeSubs 9 · pastDueSubs 2 · canceledSubs 1 · pendingInvites 2 · suspendedAccounts 2
rosaHealth past_due · rosaStoreCount 2 · willow sub past_due
```
Every metric equals an actual count over the seed (activeSubs counts active+trialing; pastDue counts past_due+unpaid+incomplete; canceled counts canceled). No hand-typed totals.

## Invariant check (temporary harness — ran then DELETED; not a permanent test)
All required GREEN writes demonstrated against the seed, exactly **5 audit rows appended**:
| Action | Result |
|---|---|
| `suspendUser('usr-anita')` | ok=true · "Anita Flores suspended" · roster status → **suspended** |
| `restoreAdmin(own-grace, sto-ivy, "Ivy Lane Pharmacy")` | ok=true · Grace roster role → **admin** |
| `restoreAdmin(own-grace, sto-ivy, "Wrong Name")` | **ok=false** · "Store-name confirmation did not match" (loud failed audit row) |
| `approve(reg-101, note)` | ok=true · invite to **dev@summitpharmacy.example** (email READ from record, never supplied) · reg-101 → **approved** |
| `reject(reg-102, reason)` | ok=true · reg-102 → **rejected** (soft; retained) |

Harness assertions all passed (audit delta = 5; each state transition verified). File removed after the run; empty temp dir cleaned; baseline `npm test` = **81/81, 11/11** (no corpse left).

## PHI / RED audit
- Grep for patient/claim/diagnosis/prescription/dollar/recovered/OwedBook/SSN/DOB/medication → **no PHI-flavored data** (the only `$` hits are `${…}` template syntax). `pharmacyLicense`/`pharmacistLicense` are business identifiers, not PHI.
- No create/delete/billing/email-edit surfaces (mocks are data only; services unchanged).

## Gates
- `npx tsc --noEmit` → **CLEAN**.
- Invariant demo → **PASS**.
- Baseline `npm test` → **81/81**.

## Scope honored
- Only `src/mocks/mission-control/store.ts` authored; service signatures FROZEN; no components, no auth changes, no deps.

## Ledger (noted, per your instruction)
- Add `server-only` package at the next dep-hygiene pass (session.ts guard was removed since it isn't installed).

🛑 STOP — awaiting approval before Sub-Phase 4 (Components).
