# stark-frontend-first — Skill Pointer

> Skill doctrine entry. The methodology lives in `SKILL.md` (Anthropic v2 format).

This skill enforces the frontend-first build: a typed service layer over mock data as the **sole swap point** for later backend integration. It forbids authoring backend code during this phase, enforces service-layer discipline, mock-data conventions, and type-driven contracts.

**Read `SKILL.md` next.**

> ⚠️ This FFM ships `SKILL.md` only. Sync the `workflow/` (00–05), `references/` (SERVICE_LAYER_PATTERNS, MOCK_DATA_PATTERNS, COMPONENT_CONVENTIONS, ANTI_PATTERNS), and `templates/` (service / mock-data / types) subfolders from the canonical copy in `cyber_pharma_v1_phase1_ffm/skills/stark-frontend-first/`. See module `MANIFEST.md`.
